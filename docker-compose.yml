version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=5000

  client:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - server
